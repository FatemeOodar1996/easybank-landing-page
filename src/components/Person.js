import React, { Component } from "react";
import css from "./Person.module.css";

class Person extends Component {
    render() {
        return <p className={css.humanname}>soll</p>;
    }
}

export default Person;
